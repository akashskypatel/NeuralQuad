# Python dependencies for the Directional documentation / website tooling.
#
# Install with:
#   python -m pip install -r requirements.txt
#
# Notes:
# - This repository is primarily a C++ project. Native build dependencies such
#   as a C++ compiler, CMake, and system libraries are not installable via pip.
# - The old Linux-specific conda export was moved to
#   `requirements-conda-linux.txt` for reference.

mkdocs>=1.6,<2
mkdocs-material>=8.5,<10
mkdocs-macros-plugin>=0.7,<2
pymdown-extensions>=9,<11
Pygments>=2.11,<3

# Optional tools mentioned in docs/website.md can be installed separately:
#   python -m pip install linkchecker
